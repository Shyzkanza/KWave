## Summary

<!-- What does this PR change, and why? -->

## Changes

-

## Checklist

- [ ] `./gradlew :kwave:jvmTest` passes
- [ ] `./gradlew :kwave:verifyRoborazziDebug` passes (or goldens re-recorded with `recordRoborazziDebug` and committed)
- [ ] `./gradlew :kwave:apiCheck` passes (or `apiDump` re-run and the `api/` dump committed for intentional API changes)
- [ ] `./gradlew detekt` passes
- [ ] Public API changes are documented (KDoc + README/CHANGELOG)

## Notes

<!-- Breaking changes, follow-ups, screenshots. -->
